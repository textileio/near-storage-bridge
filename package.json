{
  "name": "filecoin-bridge",
  "version": "0.1.0",
  "license": "(MIT AND Apache-2.0)",
  "scripts": {
    "build": "asb",
    "build:debug": "asb --target debug",
    "deploy": "npm run build && npm run deploy:testnet && npm run deploy:edge",
    "deploy:testnet": "near deploy --networkId testnet --contractName filecoin-bridge.testnet",
    "deploy:edge": "near deploy --networkId testnet --contractName filecoin-bridge-edge.testnet",
    "test": "npm run build:debug && asp"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.21.0",
    "@typescript-eslint/parser": "^4.21.0",
    "eslint": "^7.23.0",
    "eslint-plugin-jest-dom": "^3.7.0",
    "near-cli": "^1.6.0",
    "near-sdk-as": "^3.1.0",
    "nodemon": "^2.0.7",
    "typescript": "^4.2.4"
  }
}